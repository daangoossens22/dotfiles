[global]
    # monitor = 0
    follow = mouse
    enable_posix_regex = true # false
    width = 240 # 300
    # height = 300
    # notification_limit = 0
    origin = top-center # top-right
    offset = 0x8 # 10x50
    # scale = 0
    # progress_bar = true
    # progress_bar_horizontal_alignment = center
    progress_bar_height = 20 # 10
    # progress_bar_min_width = 150
    # progress_bar_max_width = 300
    progress_bar_frame_width = 0 # 1
    # indicate_hidden = true
    # transparency = 0
    # separator_height = 2
    padding = 2 # 8
    horizontal_padding = 0 # 8
    # text_icon_padding = 0
    frame_width = 2 # 3
    frame_color = "#363646CC" # "#aaaaaa"
    # separator_color = frame
    background = "#1F1F28CC"
    foreground = "#DCD7BA"
    highlight = "#7E9CD8CC"
    # sort = true
    # idle_threshold = 0
    timeout = 1 # 0
    # layer = overlay
    # force_xwayland = false
    font = JetBrains Mono 10 # Monospace 8
    # line_height = 0
    # markup = full
    # format = "<b>%s</b>\n%b"
    alignment = center # left
    vertical_alignment = center
    # show_age_threshold = 60
    # ellipsize = middle
    # ignore_newline = false
    # stack_duplicates = true
    # hide_duplicate_count = false
    # show_indicators = true
    # icon_position = left
    # min_icon_size = 0
    # max_icon_size = 32
    icon_path = /usr/share/icons/Arc/status/:/usr/share/icons/Arc/devices/ # /usr/share/icons/gnome/16x16/status/:/usr/share/icons/gnome/16x16/devices/
    enable_recursive_icon_lookup = true # false
    icon_theme = "Arc,Adwaita" # "Adwaita"
    # sticky_history = true
    # history_length = 20
    dmenu = /usr/bin/rofi -dmenu -p dunst # /usr/bin/dmenu -p dunst
    # browser = /usr/bin/xdg-open
    # always_run_script = true
    # title = Dunst
    # class = Dunst
    # force_xinerama = false
    # corner_radius = 20 # 0
    # ignore_dbusclose = false
    # mouse_left_click = close_current
    mouse_middle_click = do_action, open_url, close_current # do_action, close_current
    # mouse_right_click = close_all
    # fullscreen = pushback # show

[experimental]
    # per_monitor_dpi = false
    icon_size = 32

[urgency_low]
    frame_color = "#0000ff"
    # timeout = 10

[urgency_normal]
    # frame_color = "#00ff00"
    # timeout = 10

[urgency_critical]
    frame_color = "#ff0000"
    timeout = 0

# [progress_bar]
#     body = "progress_bar"
#     hide_text = true
#     history_ignore = true
#     fullscreen = show
#     timeout = 1
#     # origin = center
#     # offset = 0x0

# vim: ft=cfg
